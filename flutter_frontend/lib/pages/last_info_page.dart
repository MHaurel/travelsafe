import 'package:flutter/material.dart';

class LastInfoPage extends StatelessWidget {
  const LastInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Last information'),
      ),
    );
  }
}
